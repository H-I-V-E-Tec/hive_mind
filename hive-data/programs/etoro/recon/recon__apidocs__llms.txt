# eToro API Docs

- [Introduction](https://api-portal.etoro.com/index.md): Welcome to the eToro Developer Portal
- [Authentication](https://api-portal.etoro.com/core/getting-started/authentication.md): How to authenticate to the eToro API
- [Rate Limits](https://api-portal.etoro.com/core/getting-started/rate-limits.md): Understand the rate limits applied to the eToro Public API to ensure smooth and reliable integration.
- [Find Instrument ID](https://api-portal.etoro.com/core/guides/get-instrument-id.md): Learn how to resolve a ticker symbol to an eToro Instrument ID.
- [Open and close market orders](https://api-portal.etoro.com/core/guides/market-orders.md): Learn how to programmatically execute trades to enter and exit positions using the eToro API.
- [Calculate Available Cash](https://api-portal.etoro.com/core/guides/calculate-available-cash.md): Learn how to calculate your available cash balance in demo or real accounts.
- [Calculate Total Invested](https://api-portal.etoro.com/core/guides/calculate-total-invested.md): Learn how to calculate your total invested amount in demo or real accounts.
- [Calculate Profit/Loss](https://api-portal.etoro.com/core/guides/calculate-profit-loss.md): Learn how to calculate your profit/loss (unrealized PnL) in demo or real accounts.
- [Calculate Equity](https://api-portal.etoro.com/core/guides/calculate-equity.md): Learn how to calculate your equity in demo or real accounts.
- [Manage your Watchlists](https://api-portal.etoro.com/core/guides/watchlists.md): Learn how to create custom watchlists, populate them with assets, and sync your preferences across the eToro ecosystem.
- [Get Agent Portfolios](https://api-portal.etoro.com/api-reference/agent-portfolios/get-agent-portfolios.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Delete Agent Portfolio](https://api-portal.etoro.com/api-reference/agent-portfolios/delete-agent-portfolio.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Delete User Token](https://api-portal.etoro.com/api-reference/agent-portfolios/delete-user-token.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create Agent Portfolio (v2)](https://api-portal.etoro.com/api-reference/agent-portfolios/create-agent-portfolio-v2.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get Allowed Scopes (v2)](https://api-portal.etoro.com/api-reference/agent-portfolios/get-allowed-scopes-v2.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create User Token (v2)](https://api-portal.etoro.com/api-reference/agent-portfolios/create-user-token-v2.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Update User Token (v2)](https://api-portal.etoro.com/api-reference/agent-portfolios/update-user-token-v2.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Upload a media attachment](https://api-portal.etoro.com/api-reference/social-feeds/upload-a-media-attachment.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get following feed](https://api-portal.etoro.com/api-reference/social-feeds/get-following-feed.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/feeds/f…
- [Get personalised For You feed](https://api-portal.etoro.com/api-reference/social-feeds/get-personalised-for-you-feed.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/feeds/f…
- [Get instrument feed posts](https://api-portal.etoro.com/api-reference/social-feeds/get-instrument-feed-posts.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/feeds/f…
- [Get news feed](https://api-portal.etoro.com/api-reference/social-feeds/get-news-feed.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/feeds/f…
- [Get saved feed](https://api-portal.etoro.com/api-reference/social-feeds/get-saved-feed.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/feeds/f…
- [Get user feed posts](https://api-portal.etoro.com/api-reference/social-feeds/get-user-feed-posts.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/feeds/f…
- [Get user pinned feed posts](https://api-portal.etoro.com/api-reference/social-feeds/get-user-pinned-feed-posts.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/feeds/f…
- [Create a new discussion post](https://api-portal.etoro.com/api-reference/social-feeds/create-a-new-discussion-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create a poll post](https://api-portal.etoro.com/api-reference/social-feeds/create-a-poll-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Share a post](https://api-portal.etoro.com/api-reference/social-feeds/share-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get a single post by ID](https://api-portal.etoro.com/api-reference/social-feeds/get-a-single-post-by-id.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Update an existing post](https://api-portal.etoro.com/api-reference/social-feeds/update-an-existing-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Soft-delete a post](https://api-portal.etoro.com/api-reference/social-feeds/soft-delete-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List comments on a post](https://api-portal.etoro.com/api-reference/social-feeds/list-comments-on-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create a comment on a post](https://api-portal.etoro.com/api-reference/social-feeds/create-a-comment-on-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Update a comment](https://api-portal.etoro.com/api-reference/social-feeds/update-a-comment.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Soft-delete a comment](https://api-portal.etoro.com/api-reference/social-feeds/soft-delete-a-comment.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Like a comment](https://api-portal.etoro.com/api-reference/social-feeds/like-a-comment.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Unlike a comment](https://api-portal.etoro.com/api-reference/social-feeds/unlike-a-comment.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List replies on a comment](https://api-portal.etoro.com/api-reference/social-feeds/list-replies-on-a-comment.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create a reply](https://api-portal.etoro.com/api-reference/social-feeds/create-a-reply.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Update a reply](https://api-portal.etoro.com/api-reference/social-feeds/update-a-reply.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Soft-delete a reply](https://api-portal.etoro.com/api-reference/social-feeds/soft-delete-a-reply.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Like a reply](https://api-portal.etoro.com/api-reference/social-feeds/like-a-reply.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Unlike a reply](https://api-portal.etoro.com/api-reference/social-feeds/unlike-a-reply.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Follow a post](https://api-portal.etoro.com/api-reference/social-feeds/follow-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Unfollow a post](https://api-portal.etoro.com/api-reference/social-feeds/unfollow-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Like a post](https://api-portal.etoro.com/api-reference/social-feeds/like-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Unlike a post](https://api-portal.etoro.com/api-reference/social-feeds/unlike-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Pin post to profile](https://api-portal.etoro.com/api-reference/social-feeds/pin-post-to-profile.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Unpin post from profile](https://api-portal.etoro.com/api-reference/social-feeds/unpin-post-from-profile.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Vote on a poll option](https://api-portal.etoro.com/api-reference/social-feeds/vote-on-a-poll-option.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Remove poll vote](https://api-portal.etoro.com/api-reference/social-feeds/remove-poll-vote.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Save a post](https://api-portal.etoro.com/api-reference/social-feeds/save-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Unsave a post](https://api-portal.etoro.com/api-reference/social-feeds/unsave-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List shares of a post](https://api-portal.etoro.com/api-reference/social-feeds/list-shares-of-a-post.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get aggregated balances](https://api-portal.etoro.com/api-reference/balances/get-aggregated-balances.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get historical balance snapshots](https://api-portal.etoro.com/api-reference/balances/get-historical-balance-snapshots.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get balances by account type](https://api-portal.etoro.com/api-reference/balances/get-balances-by-account-type.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get historical balances by account type](https://api-portal.etoro.com/api-reference/balances/get-historical-balances-by-account-type.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get balance for a specific account](https://api-portal.etoro.com/api-reference/balances/get-balance-for-a-specific-account.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get account balance history](https://api-portal.etoro.com/api-reference/balances/get-account-balance-history.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get club dashboard data](https://api-portal.etoro.com/api-reference/clubs/get-club-dashboard-data.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get curated lists](https://api-portal.etoro.com/api-reference/watchlists/get-curated-lists.md): **Rate limit:** 60 requests per 60 seconds. This is a **dedicated** limit for this endpoint — it is **not shared** with (pooled across) any other endpoint, so the full rate is available to this endpoint alone.
- [Get market recommendations](https://api-portal.etoro.com/api-reference/watchlists/get-market-recommendations.md): **Rate limit:** 60 requests per 60 seconds. This is a **dedicated** limit for this endpoint — it is **not shared** with (pooled across) any other endpoint, so the full rate is available to this endpoint alone.
- [Get user watchlists](https://api-portal.etoro.com/api-reference/watchlists/get-user-watchlists.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/watchli…
- [Create a new watchlist](https://api-portal.etoro.com/api-reference/watchlists/create-a-new-watchlist.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Create default watchlist with selected items](https://api-portal.etoro.com/api-reference/watchlists/create-default-watchlist-with-selected-items.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Get default watchlist items](https://api-portal.etoro.com/api-reference/watchlists/get-default-watchlist-items.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/watchli…
- [Create watchlist and set as default](https://api-portal.etoro.com/api-reference/watchlists/create-watchlist-and-set-as-default.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Get user's public watchlists](https://api-portal.etoro.com/api-reference/watchlists/get-users-public-watchlists.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/watchli…
- [Get single public watchlist](https://api-portal.etoro.com/api-reference/watchlists/get-single-public-watchlist.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/watchli…
- [Change watchlist rank](https://api-portal.etoro.com/api-reference/watchlists/change-watchlist-rank.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Set watchlist as default](https://api-portal.etoro.com/api-reference/watchlists/set-watchlist-as-default.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Get single watchlist](https://api-portal.etoro.com/api-reference/watchlists/get-single-watchlist.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/watchli…
- [Rename watchlist](https://api-portal.etoro.com/api-reference/watchlists/rename-watchlist.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Delete watchlist](https://api-portal.etoro.com/api-reference/watchlists/delete-watchlist.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Update items in watchlist](https://api-portal.etoro.com/api-reference/watchlists/update-items-in-watchlist.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Add items to watchlist](https://api-portal.etoro.com/api-reference/watchlists/add-items-to-watchlist.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Remove items from watchlist](https://api-portal.etoro.com/api-reference/watchlists/remove-items-from-watchlist.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/watc…
- [Get user watchlists (BFF V2)](https://api-portal.etoro.com/api-reference/watchlists/get-user-watchlists-bff-v2.md): **Rate limit:** 60 requests per 60 seconds. This is a **dedicated** limit for this endpoint — it is **not shared** with (pooled across) any other endpoint, so the full rate is available to this endpoint alone.
- [Get an instrument's identity](https://api-portal.etoro.com/api-reference/data/get-an-instruments-identity.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get top assets for a user](https://api-portal.etoro.com/api-reference/top-assets/get-top-assets-for-a-user.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get application key data](https://api-portal.etoro.com/api-reference/app-data/get-application-key-data.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List supported exchanges](https://api-portal.etoro.com/api-reference/market-data/list-supported-exchanges.md): **Rate limit:** 120 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/market…
- [List instrument types](https://api-portal.etoro.com/api-reference/market-data/list-instrument-types.md): **Rate limit:** 120 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/market…
- [Get instrument display data](https://api-portal.etoro.com/api-reference/market-data/get-instrument-display-data.md): **Rate limit:** 120 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/market…
- [Get historical closing prices](https://api-portal.etoro.com/api-reference/market-data/get-historical-closing-prices.md): **Rate limit:** 120 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/market…
- [Get instrument market rates](https://api-portal.etoro.com/api-reference/market-data/get-instrument-market-rates.md): **Rate limit:** 120 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/market…
- [Get instrument candle history](https://api-portal.etoro.com/api-reference/market-data/get-instrument-candle-history.md): **Rate limit:** 120 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/market…
- [Search for Instruments](https://api-portal.etoro.com/api-reference/market-data/search-for-instruments.md): **Rate limit:** 120 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/market…
- [Get stocks industries](https://api-portal.etoro.com/api-reference/market-data/get-stocks-industries.md): **Rate limit:** 120 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/market…
- [Get authenticated user profile](https://api-portal.etoro.com/api-reference/identity/get-authenticated-user-profile.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List cash account transactions (paginated)](https://api-portal.etoro.com/api-reference/cash-accounts/list-cash-account-transactions-paginated.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Transfer eligibility (source and destination accounts)](https://api-portal.etoro.com/api-reference/transfer/transfer-eligibility-source-and-destination-accounts.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List transfer fees](https://api-portal.etoro.com/api-reference/transfer/list-transfer-fees.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List transfer limits](https://api-portal.etoro.com/api-reference/transfer/list-transfer-limits.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get max transferable amount](https://api-portal.etoro.com/api-reference/transfer/get-max-transferable-amount.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Execute an internal account-to-account transfer](https://api-portal.etoro.com/api-reference/transfer/execute-an-internal-account-to-account-transfer.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get transfer by ID](https://api-portal.etoro.com/api-reference/transfer/get-transfer-by-id.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List transfers by reference ID](https://api-portal.etoro.com/api-reference/transfer/list-transfers-by-reference-id.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get in-app notifications](https://api-portal.etoro.com/api-reference/notifications/get-in-app-notifications.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Mark all notifications as read](https://api-portal.etoro.com/api-reference/notifications/mark-all-notifications-as-read.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get copiers public info](https://api-portal.etoro.com/api-reference/pi-data/get-copiers-public-info.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Search for trader portfolios or Smart Portfolios by free-text query](https://api-portal.etoro.com/api-reference/portfoliosearch/search-for-trader-portfolios-or-smart-portfolios-by-free-text-query.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get price alerts](https://api-portal.etoro.com/api-reference/price-alerts/get-price-alerts.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create a price alert](https://api-portal.etoro.com/api-reference/price-alerts/create-a-price-alert.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Delete a price alert](https://api-portal.etoro.com/api-reference/price-alerts/delete-a-price-alert.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Update a price alert](https://api-portal.etoro.com/api-reference/price-alerts/update-a-price-alert.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List my OAuth applications](https://api-portal.etoro.com/api-reference/sso--applications/list-my-oauth-applications.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Register OAuth application](https://api-portal.etoro.com/api-reference/sso--applications/register-oauth-application.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Update OAuth application](https://api-portal.etoro.com/api-reference/sso--applications/update-oauth-application.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Delete OAuth application](https://api-portal.etoro.com/api-reference/sso--applications/delete-oauth-application.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Replace OAuth client secret](https://api-portal.etoro.com/api-reference/sso--applications/replace-oauth-client-secret.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List available OAuth scopes](https://api-portal.etoro.com/api-reference/sso--scopes/list-available-oauth-scopes.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create Sub-Account Money Transfer](https://api-portal.etoro.com/api-reference/sub-accounts--etoro-trading/create-sub-account-money-transfer.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List Sub-Account User Tokens](https://api-portal.etoro.com/api-reference/sub-accounts--etoro-trading/list-sub-account-user-tokens.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create Sub-Account User Token](https://api-portal.etoro.com/api-reference/sub-accounts--etoro-trading/create-sub-account-user-token.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get Allowed Sub-Account Token Scopes](https://api-portal.etoro.com/api-reference/sub-accounts--etoro-trading/get-allowed-sub-account-token-scopes.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Revoke Sub-Account User Token](https://api-portal.etoro.com/api-reference/sub-accounts--etoro-trading/revoke-sub-account-user-token.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Update Sub-Account User Token](https://api-portal.etoro.com/api-reference/sub-accounts--etoro-trading/update-sub-account-user-token.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get My Sub-Accounts](https://api-portal.etoro.com/api-reference/sub-accounts/get-my-sub-accounts.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create My Sub-Account](https://api-portal.etoro.com/api-reference/sub-accounts/create-my-sub-account.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Close demo position by units](https://api-portal.etoro.com/api-reference/trading--demo/close-demo-position-by-units.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Cancel pending demo close order](https://api-portal.etoro.com/api-reference/trading--demo/cancel-pending-demo-close-order.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Get aggregated portfolio snapshot](https://api-portal.etoro.com/api-reference/trading--demo/get-aggregated-portfolio-snapshot.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `POST /api/v1/tradin…
- [Get a filtered aggregated portfolio snapshot](https://api-portal.etoro.com/api-reference/trading--demo/get-a-filtered-aggregated-portfolio-snapshot.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Get close order information and closed position details](https://api-portal.etoro.com/api-reference/trading--demo/get-close-order-information-and-closed-position-details.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Get account PnL and portfolio details](https://api-portal.etoro.com/api-reference/trading--demo/get-account-pnl-and-portfolio-details.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Get demo portfolio breakdown](https://api-portal.etoro.com/api-reference/trading--demo/get-demo-portfolio-breakdown.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [List trading history](https://api-portal.etoro.com/api-reference/trading--demo/list-trading-history.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Modify stop-loss and take-profit settings on an open position](https://api-portal.etoro.com/api-reference/trading--demo/modify-stop-loss-and-take-profit-settings-on-an-open-position.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create an order](https://api-portal.etoro.com/api-reference/trading--demo/create-an-order.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Cancels an order before it is executed](https://api-portal.etoro.com/api-reference/trading--demo/cancels-an-order-before-it-is-executed.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Get what-if trading cost breakdown](https://api-portal.etoro.com/api-reference/trading--demo/get-what-if-trading-cost-breakdown.md): **Rate limit:** 20 requests per 60 seconds. This is a **dedicated** limit for this endpoint — it is **not shared** with (pooled across) any other endpoint, so the full rate is available to this endpoint alone.
- [Check instrument trading eligibility](https://api-portal.etoro.com/api-reference/trading--demo/check-instrument-trading-eligibility.md): **Rate limit:** 20 requests per 60 seconds. This is a **dedicated** limit for this endpoint — it is **not shared** with (pooled across) any other endpoint, so the full rate is available to this endpoint alone.
- [Get instrument breakdown (demo)](https://api-portal.etoro.com/api-reference/trading--demo/get-instrument-breakdown-demo.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Get order information and position details](https://api-portal.etoro.com/api-reference/trading--demo/get-order-information-and-position-details.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Submit an order for asynchronous processing](https://api-portal.etoro.com/api-reference/trading--demo/submit-an-order-for-asynchronous-processing.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Request cancellation of a pending order](https://api-portal.etoro.com/api-reference/trading--demo/request-cancellation-of-a-pending-order.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Close position by units](https://api-portal.etoro.com/api-reference/trading--real/close-position-by-units.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Cancel pending close order](https://api-portal.etoro.com/api-reference/trading--real/cancel-pending-close-order.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Get aggregated portfolio snapshot](https://api-portal.etoro.com/api-reference/trading--real/get-aggregated-portfolio-snapshot.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `POST /api/v1/tradin…
- [Get a filtered aggregated portfolio snapshot](https://api-portal.etoro.com/api-reference/trading--real/get-a-filtered-aggregated-portfolio-snapshot.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Get portfolio breakdown](https://api-portal.etoro.com/api-reference/trading--real/get-portfolio-breakdown.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Get close order information and closed position details](https://api-portal.etoro.com/api-reference/trading--real/get-close-order-information-and-closed-position-details.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Get account PnL and portfolio details](https://api-portal.etoro.com/api-reference/trading--real/get-account-pnl-and-portfolio-details.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [List trading history](https://api-portal.etoro.com/api-reference/trading--real/list-trading-history.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create an order](https://api-portal.etoro.com/api-reference/trading--real/create-an-order.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Cancels an order before it is executed](https://api-portal.etoro.com/api-reference/trading--real/cancels-an-order-before-it-is-executed.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Get what-if trading cost breakdown](https://api-portal.etoro.com/api-reference/trading--real/get-what-if-trading-cost-breakdown.md): **Rate limit:** 20 requests per 60 seconds. This is a **dedicated** limit for this endpoint — it is **not shared** with (pooled across) any other endpoint, so the full rate is available to this endpoint alone.
- [Check instrument trading eligibility](https://api-portal.etoro.com/api-reference/trading--real/check-instrument-trading-eligibility.md): **Rate limit:** 20 requests per 60 seconds. This is a **dedicated** limit for this endpoint — it is **not shared** with (pooled across) any other endpoint, so the full rate is available to this endpoint alone.
- [Get instrument breakdown](https://api-portal.etoro.com/api-reference/trading--real/get-instrument-breakdown.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Get order information and position details](https://api-portal.etoro.com/api-reference/trading--real/get-order-information-and-position-details.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `GET /api/v1/trading…
- [Modify stop-loss and take-profit settings on an open position](https://api-portal.etoro.com/api-reference/trading--real/modify-stop-loss-and-take-profit-settings-on-an-open-position.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Submit an order for asynchronous processing](https://api-portal.etoro.com/api-reference/trading--real/submit-an-order-for-asynchronous-processing.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Request cancellation of a pending order](https://api-portal.etoro.com/api-reference/trading--real/request-cancellation-of-a-pending-order.md): **Rate limit:** 20 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `DELETE /api/v1/trad…
- [Get user profile and account summary](https://api-portal.etoro.com/api-reference/users-info/get-user-profile-and-account-summary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Search users](https://api-portal.etoro.com/api-reference/users-info/search-users.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get user daily performance](https://api-portal.etoro.com/api-reference/users-info/get-user-daily-performance.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get user performance history](https://api-portal.etoro.com/api-reference/users-info/get-user-performance-history.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get user live portfolio](https://api-portal.etoro.com/api-reference/users-info/get-user-live-portfolio.md): **Rate limit:** 60 requests per 60 seconds. This is a **dedicated** limit for this endpoint — it is **not shared** with (pooled across) any other endpoint, so the full rate is available to this endpoint alone.
- [Get user trade info](https://api-portal.etoro.com/api-reference/users-info/get-user-trade-info.md): **Rate limit:** 60 requests per 60 seconds. This is a **dedicated** limit for this endpoint — it is **not shared** with (pooled across) any other endpoint, so the full rate is available to this endpoint alone.
- [Get paginated investor rankings](https://api-portal.etoro.com/api-reference/rankings/get-paginated-investor-rankings.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Bulk-fetch ranking rows](https://api-portal.etoro.com/api-reference/rankings/bulk-fetch-ranking-rows.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List available ranking presets](https://api-portal.etoro.com/api-reference/rankings/list-available-ranking-presets.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get rankings by named preset](https://api-portal.etoro.com/api-reference/rankings/get-rankings-by-named-preset.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get ranking universe aggregated by sector or industry](https://api-portal.etoro.com/api-reference/rankings/get-ranking-universe-aggregated-by-sector-or-industry.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [List all ranking tags](https://api-portal.etoro.com/api-reference/rankings/list-all-ranking-tags.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get ranking row for a single investor](https://api-portal.etoro.com/api-reference/rankings/get-ranking-row-for-a-single-investor.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get asset allocation history](https://api-portal.etoro.com/api-reference/user-stats/get-asset-allocation-history.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get portfolio copier stats](https://api-portal.etoro.com/api-reference/user-stats/get-portfolio-copier-stats.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get market exposure history](https://api-portal.etoro.com/api-reference/user-stats/get-market-exposure-history.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get investor gain time-series](https://api-portal.etoro.com/api-reference/user-stats/get-investor-gain-time-series.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Start copying an investor, or adjust an existing copy](https://api-portal.etoro.com/api-reference/copy-trading/start-copying-an-investor-or-adjust-an-existing-copy.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Close or detach a copy relationship](https://api-portal.etoro.com/api-reference/copy-trading/close-or-detach-a-copy-relationship.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Close or detach a copy relationship (JSON body)](https://api-portal.etoro.com/api-reference/copy-trading/close-or-detach-a-copy-relationship-json-body.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Pre-check whether a copy registration would be allowed](https://api-portal.etoro.com/api-reference/copy-trading/pre-check-whether-a-copy-registration-would-be-allowed.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Poll a copy operation's outcome](https://api-portal.etoro.com/api-reference/copy-trading/poll-a-copy-operations-outcome.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Start copying an investor, or adjust an existing copy](https://api-portal.etoro.com/api-reference/copy-trading--demo/start-copying-an-investor-or-adjust-an-existing-copy.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Close or detach a copy relationship](https://api-portal.etoro.com/api-reference/copy-trading--demo/close-or-detach-a-copy-relationship.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Close or detach a copy relationship (JSON body)](https://api-portal.etoro.com/api-reference/copy-trading--demo/close-or-detach-a-copy-relationship-json-body.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Pre-check whether a copy registration would be allowed](https://api-portal.etoro.com/api-reference/copy-trading--demo/pre-check-whether-a-copy-registration-would-be-allowed.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Poll a copy operation's outcome](https://api-portal.etoro.com/api-reference/copy-trading--demo/poll-a-copy-operations-outcome.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create Agent Portfolio](https://api-portal.etoro.com/deprecated-api-reference/agent-portfolios/create-agent-portfolio.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Create User Token](https://api-portal.etoro.com/deprecated-api-reference/agent-portfolios/create-user-token.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Update User Token](https://api-portal.etoro.com/deprecated-api-reference/agent-portfolios/update-user-token.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Get instrument feed posts](https://api-portal.etoro.com/deprecated-api-reference/social-feeds/get-instrument-feed-posts.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Create a new discussion post](https://api-portal.etoro.com/deprecated-api-reference/social-feeds/create-a-new-discussion-post.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Get user feed posts](https://api-portal.etoro.com/deprecated-api-reference/social-feeds/get-user-feed-posts.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Create a comment on a post](https://api-portal.etoro.com/deprecated-api-reference/social-feeds/create-a-comment-on-a-post.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Place demo limit order (market-if-touched)](https://api-portal.etoro.com/deprecated-api-reference/trading--demo/place-demo-limit-order-market-if-touched.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Cancel pending demo limit order](https://api-portal.etoro.com/deprecated-api-reference/trading--demo/cancel-pending-demo-limit-order.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Open demo order by amount](https://api-portal.etoro.com/deprecated-api-reference/trading--demo/open-demo-order-by-amount.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Open demo market order by units](https://api-portal.etoro.com/deprecated-api-reference/trading--demo/open-demo-market-order-by-units.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Cancel pending demo open order](https://api-portal.etoro.com/deprecated-api-reference/trading--demo/cancel-pending-demo-open-order.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Get order information](https://api-portal.etoro.com/deprecated-api-reference/trading--demo/get-order-information.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Place limit order (market-if-touched)](https://api-portal.etoro.com/deprecated-api-reference/trading--real/place-limit-order-market-if-touched.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Cancel pending limit order](https://api-portal.etoro.com/deprecated-api-reference/trading--real/cancel-pending-limit-order.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Open market order by cash amount](https://api-portal.etoro.com/deprecated-api-reference/trading--real/open-market-order-by-cash-amount.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Open market order by units](https://api-portal.etoro.com/deprecated-api-reference/trading--real/open-market-order-by-units.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Cancel pending open order](https://api-portal.etoro.com/deprecated-api-reference/trading--real/cancel-pending-open-order.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Get order information](https://api-portal.etoro.com/deprecated-api-reference/trading--real/get-order-information.md): Deprecated: This endpoint is marked deprecated in the eToro Public API Swagger. Prefer the current replacement endpoints where available.
- [Overview](https://api-portal.etoro.com/core/websocket/overview.md): Overview of the eToro WebSocket API
- [Authentication](https://api-portal.etoro.com/core/websocket/authentication.md): Authentication of the eToro WebSocket API
- [Topics](https://api-portal.etoro.com/core/websocket/topics.md): Topics for the eToro WebSocket API
- [Example code](https://api-portal.etoro.com/core/websocket/example-code.md): Example code for the eToro WebSocket API
- [Control channel (request / response)](https://api-portal.etoro.com/core/websocket/notifications/websockets/control-channel-request-response.md): All client requests share one JSON envelope: `{ "id": "<guid>", "operation": "<Authenticate|Subscribe|Unsubscribe>", "data": { ... } }`. The server answers each request on the same connection with an acknowledgement that echoes the `id` and the `operation`. The `id` is a client-generated GUID used t…
- [Instrument candles](https://api-portal.etoro.com/core/websocket/notifications/websockets/instrument-candles.md): Subscribe with topic `candle:<instrumentId>` (e.g. `candle:100000`). Each push carries a `content` field that is a JSON-encoded string of a `Trading.Instrument.Candle` object (see schema). No authentication required.
- [Instrument rates](https://api-portal.etoro.com/core/websocket/notifications/websockets/instrument-rates.md): Subscribe with topic `instrument:<instrumentId>` (e.g. `instrument:100000`). Each push carries a `content` field that is a JSON-encoded string of a `Trading.Instrument.Rate` object (see schema).
- [Private portfolio updates](https://api-portal.etoro.com/core/websocket/notifications/websockets/private-portfolio-updates.md): Subscribe with topic `private` (requires a prior successful Authenticate). Each push carries a `content` field that is a JSON-encoded string of a trading order update (e.g. `Trading.OrderForOpen.Update`, `Trading.OrderForCloseMultiple.Update`).
- [Base44](https://api-portal.etoro.com/core/vibe-code/base44.md): Create custom eToro apps using Base44's no-code platform
- [Cursor](https://api-portal.etoro.com/core/vibe-code/cursor.md): Integrate the eToro Public API into Cursor using MCP
- [Claude Code](https://api-portal.etoro.com/core/vibe-code/claude-code.md): Integrate the eToro Public API into Claude Code using MCP
- [Windsurf](https://api-portal.etoro.com/core/vibe-code/windsurf.md): Integrate the eToro Public API into Windsurf IDE
- [Antigravity](https://api-portal.etoro.com/core/vibe-code/antigravity.md): Integrate the eToro Public API into Antigravity IDE
- [SKILL.md](https://api-portal.etoro.com/core/ai-agents/etoro-skill.md): Agent Skill for accessing the eToro API
- [Core API Changelog](https://api-portal.etoro.com/core/changelog.md): New endpoints, capabilities, and breaking changes in the eToro Public API.
- [Introduction](https://api-portal.etoro.com/partners/introduction.md): Welcome to the eToro Partners API
- [Initiate GDPR user deletion](https://api-portal.etoro.com/partners-api-reference/ops/initiate-gdpr-user-deletion.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get application identifiers dictionary](https://api-portal.etoro.com/partners-api-reference/ops/get-application-identifiers-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get document sides dictionary](https://api-portal.etoro.com/partners-api-reference/ops/get-document-sides-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get document subtypes dictionary](https://api-portal.etoro.com/partners-api-reference/ops/get-document-subtypes-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get document types dictionary](https://api-portal.etoro.com/partners-api-reference/ops/get-document-types-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get EV statuses dictionary](https://api-portal.etoro.com/partners-api-reference/ops/get-ev-statuses-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get screening statuses dictionary](https://api-portal.etoro.com/partners-api-reference/ops/get-screening-statuses-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Upload document](https://api-portal.etoro.com/partners-api-reference/ops/upload-document.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Download document](https://api-portal.etoro.com/partners-api-reference/ops/download-document.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get customer documents](https://api-portal.etoro.com/partners-api-reference/ops/get-customer-documents.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Add document classification](https://api-portal.etoro.com/partners-api-reference/ops/add-document-classification.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get electronic verification status](https://api-portal.etoro.com/partners-api-reference/ops/get-electronic-verification-status.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Send user to electronic verification](https://api-portal.etoro.com/partners-api-reference/ops/send-user-to-electronic-verification.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get screening status](https://api-portal.etoro.com/partners-api-reference/ops/get-screening-status.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Send user to screening](https://api-portal.etoro.com/partners-api-reference/ops/send-user-to-screening.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get KYC Questionnaire](https://api-portal.etoro.com/partners-api-reference/kyc/get-kyc-questionnaire.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Submit KYC Questionnaire Answers](https://api-portal.etoro.com/partners-api-reference/kyc/submit-kyc-questionnaire-answers.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get Single KYC Questionnaire Question](https://api-portal.etoro.com/partners-api-reference/kyc/get-single-kyc-questionnaire-question.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Submit Single KYC Questionnaire Answer](https://api-portal.etoro.com/partners-api-reference/kyc/submit-single-kyc-questionnaire-answer.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get KYC Fields](https://api-portal.etoro.com/partners-api-reference/kyc/get-kyc-fields.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get requirement categories dictionary](https://api-portal.etoro.com/partners-api-reference/kyc/get-requirement-categories-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get requirement overview statuses dictionary](https://api-portal.etoro.com/partners-api-reference/kyc/get-requirement-overview-statuses-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get requirement status reasons dictionary](https://api-portal.etoro.com/partners-api-reference/kyc/get-requirement-status-reasons-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get requirement types dictionary](https://api-portal.etoro.com/partners-api-reference/kyc/get-requirement-types-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get user interaction types dictionary](https://api-portal.etoro.com/partners-api-reference/kyc/get-user-interaction-types-dictionary.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get customer KYC gaps](https://api-portal.etoro.com/partners-api-reference/kyc/get-customer-kyc-gaps.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get KYC questions-answers mapping](https://api-portal.etoro.com/partners-api-reference/kyc/get-kyc-questions-answers-mapping.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get KYC question-answers mapping by question ID](https://api-portal.etoro.com/partners-api-reference/kyc/get-kyc-question-answers-mapping-by-question-id.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get KYC Regulations](https://api-portal.etoro.com/partners-api-reference/kyc/get-kyc-regulations.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get KYC Address](https://api-portal.etoro.com/partners-api-reference/kyc/get-kyc-address.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Update KYC Address](https://api-portal.etoro.com/partners-api-reference/kyc/update-kyc-address.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get Contact Info](https://api-portal.etoro.com/partners-api-reference/kyc/get-contact-info.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get KYC Personal Details](https://api-portal.etoro.com/partners-api-reference/kyc/get-kyc-personal-details.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Update KYC Personal Details](https://api-portal.etoro.com/partners-api-reference/kyc/update-kyc-personal-details.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get KYC Fields (V2)](https://api-portal.etoro.com/partners-api-reference/kyc/get-kyc-fields-v2.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get KYC Metadata Mappings](https://api-portal.etoro.com/partners-api-reference/kyc/get-kyc-metadata-mappings.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get Verification Level 3 Gaps](https://api-portal.etoro.com/partners-api-reference/kyc/get-verification-level-3-gaps.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get user cash accounts](https://api-portal.etoro.com/partners-api-reference/cash-accounts/get-user-cash-accounts.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create a cash account](https://api-portal.etoro.com/partners-api-reference/cash-accounts/create-a-cash-account.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get cash account creation eligibility](https://api-portal.etoro.com/partners-api-reference/cash-accounts/get-cash-account-creation-eligibility.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get cash account creation status](https://api-portal.etoro.com/partners-api-reference/cash-accounts/get-cash-account-creation-status.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get cash account by id](https://api-portal.etoro.com/partners-api-reference/cash-accounts/get-cash-account-by-id.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get account-specific first-time deposit(s)](https://api-portal.etoro.com/partners-api-reference/ftd/get-account-specific-first-time-deposits.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Notify deposit from partner (async)](https://api-portal.etoro.com/partners-api-reference/ftd/notify-deposit-from-partner-async.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get global first-time deposit](https://api-portal.etoro.com/partners-api-reference/ftd/get-global-first-time-deposit.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Initiate a crypto deposit intent](https://api-portal.etoro.com/partners-api-reference/crypto-deposit/initiate-a-crypto-deposit-intent.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Lookup crypto deposit by request reference ID](https://api-portal.etoro.com/partners-api-reference/crypto-deposit/lookup-crypto-deposit-by-request-reference-id.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Submit trading account withdrawal](https://api-portal.etoro.com/partners-api-reference/crypto-withdrawals/submit-trading-account-withdrawal.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Trading account withdrawal approval process eligibility](https://api-portal.etoro.com/partners-api-reference/crypto-withdrawals/trading-account-withdrawal-approval-process-eligibility.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Trading account withdrawal eligibility](https://api-portal.etoro.com/partners-api-reference/crypto-withdrawals/trading-account-withdrawal-eligibility.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Trading account withdrawal status](https://api-portal.etoro.com/partners-api-reference/crypto-withdrawals/trading-account-withdrawal-status.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Partner registers a new eToro user](https://api-portal.etoro.com/partners-api-reference/registration/partner-registers-a-new-etoro-user.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Get registration eligibility](https://api-portal.etoro.com/partners-api-reference/registration/get-registration-eligibility.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Create Account](https://api-portal.etoro.com/partners-api-reference/sub-accounts/create-account.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Delete Accounts](https://api-portal.etoro.com/partners-api-reference/sub-accounts/delete-accounts.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Send email verification code](https://api-portal.etoro.com/partners-api-reference/email-verification/send-email-verification-code.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `POST /api/v1/users/…
- [Verify email with OTP code](https://api-portal.etoro.com/partners-api-reference/email-verification/verify-email-with-otp-code.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `POST /api/v1/users/…
- [Direct email verification (trusted partner)](https://api-portal.etoro.com/partners-api-reference/email-verification/direct-email-verification-trusted-partner.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `POST /api/v1/users/…
- [Initiate phone verification](https://api-portal.etoro.com/partners-api-reference/phone-verification/initiate-phone-verification.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `POST /api/v1/users/…
- [Verify phone with verification code](https://api-portal.etoro.com/partners-api-reference/phone-verification/verify-phone-with-verification-code.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `POST /api/v1/users/…
- [Direct phone verification (trusted partner)](https://api-portal.etoro.com/partners-api-reference/phone-verification/direct-phone-verification-trusted-partner.md): **Rate limit:** 60 requests per 60 seconds. This is a **shared quota** — the same budget is consumed by a group of related endpoints, so calling any of them reduces what is left for the others (you cannot call each at the full rate independently). Endpoints sharing this quota: - `POST /api/v1/users/…
- [Submit trusted partner verification](https://api-portal.etoro.com/partners-api-reference/verification/submit-trusted-partner-verification.md): **Rate limit:** 60 requests per 60 seconds. This is the **default shared quota** — it is shared with every other endpoint that has no dedicated limit, so requests across those endpoints all draw from the same budget.
- [Partners API Changelog](https://api-portal.etoro.com/partners/changelog.md): New endpoints, capabilities, and breaking changes in the eToro Partners API.

## OpenAPI Specs

- [deprecated-openapi](/api-reference/deprecated-openapi.json)
- [openapi](/api-reference/openapi.json)
- [partners-openapi](/api-reference/partners-openapi.json)

## AsyncAPI Specs

- [asyncapi](/api-reference/asyncapi.json)
